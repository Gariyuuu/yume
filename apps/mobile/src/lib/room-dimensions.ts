// Must match apps/web/src/components/room-canvas/room-dimensions.ts — the
// room coordinate space is shared across platforms even though each
// renders it at a different on-screen scale.
export const ROOM_WIDTH = 1200;
export const ROOM_HEIGHT = 800;
