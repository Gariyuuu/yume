// Shared between the Konva decoration canvas and the DOM participant-bubble
// overlay so both layers agree on the room's coordinate space.
export const ROOM_WIDTH = 1200;
export const ROOM_HEIGHT = 800;
